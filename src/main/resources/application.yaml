server:
  port: 8090

logging:
  level:
    com.ai.commentbot: DEBUG
    org.springframework.security: TRACE
    org.springframework.security.outh2: TRACE
    org.springframework.web: TRACE
    org.springframework.web.reactive: TRACE

open-ai:
  secret-key: #provide the secret key here
  chat-endpoint: https://api.openai.com/v1/chat/completions

spring:
  #Local MongoDB config
  data:
    mongodb:
#      authentication-database: admin
#      username: root
#      password: root
      database: shreeramLearning
      port: 27017
      host: localhost

